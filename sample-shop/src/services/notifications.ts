/** Envía el correo de confirmación con el total cobrado. (Stub para el MVP.) */
export function sendConfirmationEmail(email: string, total: number): void {
  console.log(`[email] a ${email}: tu total es $${total}`);
}
